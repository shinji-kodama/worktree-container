# yaml-language-server: $schema=https://aka.ms/winget-manifest.version.1.9.0.schema.json
# ─────────────────────────────────────────────────────────────────────
# WinGet Version Manifest for worktree-container
#
# Directory in winget-pkgs:
#   manifests/s/mmr-tortoise/worktree-container/{{VERSION}}/
#
# File name: mmr-tortoise.worktree-container.yaml
# ─────────────────────────────────────────────────────────────────────
PackageIdentifier: mmr-tortoise.worktree-container
PackageVersion: "{{VERSION}}"
DefaultLocale: en-US
ManifestType: version
ManifestVersion: 1.9.0
